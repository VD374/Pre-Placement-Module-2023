class Solution(object):
    def singleNumber(self, nums):
       for i in range(len(nums)):
        if nums.count(nums[i])==1:
#counting the occurance of every element in the given array
          return nums[i]
        
