class Solution(object):
    def search(self, nums, target):
        n=len(nums)
        for k in range(n):
         if nums[n-k%n:]==nums[:n-k%n]:
          return k
         else:
            return nums[k]
