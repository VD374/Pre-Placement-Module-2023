class Solution(object):
    def search(self, nums, target):
        num=sorted(nums)
        dic={}
        for i in range(len(nums)):
            dic[nums[i]]=i
        try:
            return dic[target]
        except:
            return -1
